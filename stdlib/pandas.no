# ============================================================
# NOVA STANDARD LIBRARY - PANDAS DATAFRAMES (pandas)
# ============================================================
import pandas as pd

show "=== Nova In-Memory DataFrame Operations ==="
df = pd.DF()
df.addCol("score", [88.0, 92.0, 79.0, 95.0])
show "Dataset shape:", df.shape()
show "DataFrame column 'score' added successfully"
