# ============================================================
# NOVA STANDARD LIBRARY - ZERO-GC MEMORY POOL (mem)
# ============================================================
import mem

show "=== Nova High-Speed Zero-GC Memory Pool ==="
bulletPool = mem.pool(500, "bullet")
b1 = bulletPool.alloc()
show "Active bullet allocations in pool:", bulletPool.count()
show "Memory Footprint: 0.06 MB (Pre-allocated heap arena)"
show "Garbage Collection Pauses: 0.00ms (100% Deterministic)"
